#
# Mobile Quick logic bridge configuration
config VX6B3E_BRIDGE
	boolean "VX6B3E chip"
	depends on I2C && I2C_PXA
	default n
	help
	  If you say yes here, you support for it.

	  This driver can NOT be built as a module.
